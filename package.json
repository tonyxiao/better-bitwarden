{
  "name": "better-bitwarden",
  "version": "0.1.0",
  "description": "A better Bitwarden CLI — sends, attachments, archiving, and more",
  "bin": {
    "bbw": "./src/index.ts"
  },
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts"
  },
  "dependencies": {
    "citty": "^0.1.6",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
